# Host 1 IP address
ip addr add 10.0.1.1/24 dev eth0

# Gateway
export GATEWAY="10.0.1.254"

# Default gateway
ip route add 10.0.0.0/24 via "$GATEWAY"

# Initialization
/shared/scripts/init.sh

# Local DNS
echo "10.0.0.1 honeymoon.com honeymoon server" >> /etc/hosts

